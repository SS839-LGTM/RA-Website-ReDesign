{
  "name": "ra-faq-proxy",
  "version": "1.0.0",
  "description": "Simple proxy to fetch RA FAQs and expose them with CORS for local static sites",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^3.4.0"
  },
  "engines": { "node": ">=18" }
}